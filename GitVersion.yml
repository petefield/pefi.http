assembly-versioning-scheme: MajorMinorPatch
next-version: 1.0.0
branches:
  main:
    regex: ^main$
    label: ''
    increment: Patch
    is-release-branch: true
  pull-request:
    label: pr
    increment: Inherit
ignore:
  sha: []
